/** Formats an ISO timestamp as a human-friendly relative time. */
export declare function formatRelativeTime(isoString: string | null | undefined): string;
/** Formats an ISO timestamp as a readable date/time string. */
export declare function formatTimestamp(isoString: string | null | undefined): string;
/** Truncates a string with an ellipsis. */
export declare function truncate(s: string, maxLen: number): string;
